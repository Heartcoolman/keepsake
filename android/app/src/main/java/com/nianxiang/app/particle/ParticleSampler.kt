/** PUBLIC STUB — image→particle sampling (budgeting, target positions, colors)
 *  lives in the private core module. Nothing outside the real renderer references it. */
package com.nianxiang.app.particle
